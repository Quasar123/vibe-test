# Copy to docker/.env.infra and adjust values for local infrastructure.
POSTGRES_USER=vibetest
POSTGRES_PASSWORD=changeme
POSTGRES_DB=vibetest
POSTGRES_PORT=5432

# Connection string for locally running VibeTest.Server (not used by compose itself):
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=vibetest;Username=vibetest;Password=changeme
