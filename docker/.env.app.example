# Copy to docker/.env.app and adjust values for the full Docker stack.
POSTGRES_USER=vibetest
POSTGRES_PASSWORD=changeme
POSTGRES_DB=vibetest
POSTGRES_PORT=5432

WEB_PORT=8080
WEB_ORIGIN=http://localhost:8080

# Change in production:
JWT_KEY=dev-secret-key-change-in-production-32chars!
